Thank you — this is exactly why fresh eyes are so valuable here. Your first exercise exposed assumptions that our own environment had hidden, and your distinction between reproduced failures and fragile patterns made the report especially useful.

The Battleship testing came with no expectation of anything in return. Your report is already a useful contribution, and any further checking of these workflows is entirely optional.

Codex reproduced the false remote report and the link check's false success, then prepared and tested fixes for both repositories. Remote detection no longer depends on ripgrep. The verification scripts now stop explicitly when it is missing, and negative searches distinguish “no match” from a tool error. The link check also propagates extraction and file-enumeration failures. The starting guides now distinguish the tools needed to run the first exercise from those it may report missing for later work.

One detail did not reproduce: running the reported commit's doctor directly returned exit status 1 with BLOCKED, as its source specifies. That behavior is now covered by a regression test and documented. If you still see status 0, the exact invocation and the status captured immediately afterwards would help clarify the difference.

The verification notes for the [starter kit](https://github.com/oikumene-works/fdroid-community-testing-starter-kit/blob/main/docs/missing-tool-review.md) and [adaptive seed](https://github.com/oikumene-works/fdroid-community-testing-adaptive-seed/blob/main/docs/missing-tool-review.md) describe the local tests and their limits. Your offer to check the patch on the same host is welcome if convenient, with no expectation of another round. We can keep this follow-up in these issues and leave the Battleship MR to its own review.
